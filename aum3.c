#include<stdio.h>
int main()
{
    int a;
printf("enter the value \n");
scanf("%d",&a);
    if(9<a  && a<100)
    {
        printf("above number is between 100");
    }
    else
    {
        printf("number is not between 10 to 100");
    }


return 0;
}