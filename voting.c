#include<stdio.h>

int main()

{
    int age ;

    printf("Enter the age of the person : \n");
    scanf("%d" , &age);

    18<=age ? printf("above person can vote") : printf("above person can not vote");
    return 0;
}